name: Hourly Update - m3u8 links & playlist

on:
  schedule:
    - cron: '30 * * * *'  # Every hour at :30 UTC (~ :00 IST)

  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-playlist:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install requirements
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi

      - name: Install Playwright browsers
        run: |
          python -m playwright install-deps
          python -m playwright install chromium --with-deps

      - name: Run m3u8 fetch & playlist generator
        run: python main.py
        env:
          INPUT_EXCEL: Cartoonxlsx.xlsx
          OUTPUT_EXCEL: updated_videos_with_links.xlsx
          OUTPUT_M3U: all_channels_playlist.m3u8

      - name: Commit updated files (Excel + m3u8)
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore(auto): updated m3u8 links & playlist ${{ github.run_number }}"
          file_pattern: |
            *.m3u8
            *.xlsx            
          skip_dirty_check: false
          skip_fetch: false
          skip_commit: false
          commit_user_name: github-actions[bot]
          commit_user_email: github-actions[bot]@users.noreply.github.com
          commit_author: github-actions[bot] <github-actions[bot]@users.noreply.github.com>

      - name: Debug - List output files
        run: |
          echo "Files in repo now:"
          ls -la *.xlsx *.m3u8 || echo "No output files found — check script logs above"
